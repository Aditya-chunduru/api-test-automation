name: API Performance & Regression Gate

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  load-test-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install locust python-dotenv requests urllib3

      - name: Run Locust Headless Load Test
        env:
          SUPABASE_URL: https://aommnxekaskgjlwjsdpc.supabase.co
          SUPABASE_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }} # dual-key parity for dotenv/env fallback
        run: |
          # -u 50: Stress concurrency (drop to -u 15 if free-tier rate limits trigger 429s)
          # -r 5: Gentle user ramp-up (5/sec) to prevent initial socket storms
          locust -f tests/locustfile.py \
            --headless \
            -u 50 \
            -r 5 \
            --run-time 30s \
            --csv=load_test_results \
            --host "$SUPABASE_URL"

      - name: Evaluate Error Rate Gate
        run: |
          # Process exit code is enforced via test_stop hook in locustfile.py;
          # this step dumps telemetry summary for PR build logs review.
          STATS_FILE="load_test_results_stats.csv"
          if [ -f "$STATS_FILE" ]; then
            echo "--- Load Test Stats Summary ---"
            cat "$STATS_FILE"
          else
            echo "Warning: Stats CSV not found."
          fi
